<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/navbar.ejs') %>
    <div class="content">
      <% if (users.length > 0) { %> <% users.forEach(user => { %>
      <h3 class="title"><%= user.Number_1 %> + <%= user.Number_2 %></h3>
      =
      <p><%= user.Result %></p>
      <% }) %> <% } else { %>
      <p>Não há usuários para exibir...</p>
      <% } %>
    </div>
  </body>
</html>
